<Page x:Class="Attractions.Pages.CartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="Корзина"
      Background="#20331B">

    <Page.Resources>
        <!-- Цвета -->
        <Color x:Key="PrimaryColor">#A4B17B</Color>
        <Color x:Key="AccentColor">#697E50</Color>
        <Color x:Key="DarkColor">#20331B</Color>
        <Color x:Key="DangerColor">#9C2251</Color>

        <!-- Стили кнопок -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#FFA4B17B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                BorderBrush="Transparent" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для опасных кнопок -->
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#FF9C2251"/>
        </Style>

        <!-- Стиль для ListView -->
        <Style TargetType="ListView">
            <Setter Property="Background" Value="#A4B17B"/>
            <Setter Property="Foreground" Value="#20331B"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <!-- Стиль для текста -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#A4B17B"/>
        </Style>

        <!-- Стиль для CheckBox -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#A4B17B"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Page.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Навигационная панель -->
        <Border Grid.Row="0" Background="#697E50" Padding="10" CornerRadius="10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="🏠 Главная" Click="BtnMain_Click" Width="120" Height="30" Foreground="#FF20331B"/>
                <Button Content="🛒 Корзина" Click="BtnCart_Click" Width="120" Height="30" Foreground="#FF20331B"/>
                <Button Content="★ Избранное" Click="BtnFavorites_Click" Width="120" Height="30" Foreground="#FF20331B"/>
                <Button Content="👤 Аккаунт" Click="BtnAccount_Click" Width="120" Height="30" Foreground="#FF20331B"/>
                <Button Content="Выйти" Click="BtnLogout_Click" Width="120" Style="{StaticResource DangerButton}" Height="30"/>
            </StackPanel>
        </Border>

        <!-- Заголовок -->
        <TextBlock Grid.Row="1" Text="Ваша корзина" FontSize="24" FontWeight="Bold" Foreground="#A4B17B" Margin="0,10,0,10"/>

        <!-- Список товаров -->
        <ListView x:Name="lvCart" Grid.Row="2" Background="#697E50">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="BorderBrush" Value="#A4B17B"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,0,0,5"/>
                    <Setter Property="Background" Value="#A4B17B"/>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Чекбокс выбора -->
                        <CheckBox Grid.Column="0" VerticalAlignment="Center" Margin="5,0,10,0"
                          IsChecked="{Binding IsSelected}" Checked="CheckBox_CheckedChanged" Unchecked="CheckBox_CheckedChanged"/>

                        <!-- Информация о товаре -->
                        <StackPanel Grid.Column="1">
                            <StackPanel Orientation="Horizontal">
                                <Border CornerRadius="5" BorderThickness="1" BorderBrush="#20331B">
                                    <Image Source="{Binding Entertainment.CurrentPhoto}" Width="100" Height="80" Stretch="UniformToFill" Margin="0,0,10,0"/>
                                </Border>
                                <StackPanel>
                                    <TextBlock Text="{Binding Entertainment.Name}" FontWeight="Bold" Foreground="#20331B"/>
                                    <TextBlock Text="{Binding Schedule.Date, StringFormat='Дата: {0:dd.MM.yyyy}'}" Foreground="#20331B"/>
                                    <TextBlock Text="{Binding Schedule.Time, StringFormat='Время: {0:hh\\:mm}'}" Foreground="#20331B"/>
                                    <TextBlock Text="{Binding Entertainment.Price, StringFormat='Цена: {0:N} ₽'}" FontWeight="Bold" Foreground="#20331B"/>
                                    <TextBlock Text="{Binding Status}" Foreground="{Binding StatusColor}" Margin="0,5,0,0"/>
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>

                        <!-- Кнопки действий -->
                        <StackPanel Grid.Column="2" Orientation="Vertical" HorizontalAlignment="Right">
                            <Button Content="Удалить" Width="100" Margin="0,0,0,5"
                            Click="BtnRemove_Click" Tag="{Binding Schedule.ScheduleId}"/>
                            <Button Content="В избранное" Width="100"
                            Click="BtnAddToFavorites_Click" Tag="{Binding Entertainment.EntertainmentId}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Панель итогов и кнопок -->
        <Border Grid.Row="3" Background="#697E50" Padding="10" Margin="0,10,0,0" CornerRadius="5">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,20,0">
                    <CheckBox x:Name="cbSelectAll" Content="Выбрать все" VerticalAlignment="Center" Margin="0,0,10,0"
                      Checked="CbSelectAll_Checked" Unchecked="CbSelectAll_Unchecked" Foreground="#FF20331B"/>
                    <TextBlock Text="{Binding TotalPrice, StringFormat='Итого: {0:N} ₽'}" FontSize="16" FontWeight="Bold" Foreground="#20331B"/>
                </StackPanel>
                <Button x:Name="btnCheckout" Content="Перейти к оформлению" Width="180" Height="35"
                Click="BtnCheckout_Click" IsEnabled="{Binding HasSelectedItems}"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
